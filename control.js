// este codigonos sirve para tener varios selcionadores de sedes.
window.addEventListener("DOMContentLoaded", () => {
    const el = document.getElementById("sedes");
    el.addEventListener("click", () => {
       let seleccionados = document.getElementById('sedes').selectedOptions; 
        for(let i = 0;  i < seleccionados.length; i++){
          console.log(seleccionados[i].value);
          }
      });
    });
  
   function estaderoElFlowDeMael() {
  características = Promise
  
   };
  